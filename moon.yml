$schema: "https://moonrepo.dev/schemas/project.json"

language: go
layer: library

tasks:
  build:
    command: 'go build ./...'
  test:
    command: 'go test ./...'
  lint:
    command: 'go vet ./...'
